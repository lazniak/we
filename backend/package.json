{
  "name": "we-pablogfx-backend",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "nanoid": "^5.0.7"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "jszip": "^3.10.1",
    "typescript": "^5.6.0"
  }
}
